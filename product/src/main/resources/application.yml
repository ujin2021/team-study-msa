spring:
  application:
    name: product-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: {spring.application.name}

  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-db

server:
  port: 8082